import { GetTrailerPipe } from './has-trailer.pipe';

describe('GetTrailerPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTrailerPipe();
    expect(pipe).toBeTruthy();
  });
});
