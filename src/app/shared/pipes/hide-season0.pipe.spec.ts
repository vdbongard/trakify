import { HideSeason0Pipe } from './hide-season0.pipe';

describe('HideSeason0Pipe', () => {
  it('create an instance', () => {
    const pipe = new HideSeason0Pipe();
    expect(pipe).toBeTruthy();
  });
});
