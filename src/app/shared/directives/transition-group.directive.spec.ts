import { TransitionGroupDirective } from './transition-group.directive';

describe('TransitionGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionGroupDirective();
    expect(directive).toBeTruthy();
  });
});
