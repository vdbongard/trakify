import { SeasonLinkWithCounterPipe } from './season-link-with-counter.pipe';

describe('SeasonLinkWithCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonLinkWithCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
