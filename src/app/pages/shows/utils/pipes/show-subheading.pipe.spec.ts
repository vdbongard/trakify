import { ShowSubheadingPipe } from './show-subheading.pipe';

describe('ShowSubheadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSubheadingPipe();
    expect(pipe).toBeTruthy();
  });
});
