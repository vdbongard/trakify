import { EpisodeProgressPipe } from './episode-progress.pipe';

describe('EpisodeProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new EpisodeProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
