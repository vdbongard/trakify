import { Season0AsSpecialsPipe } from './season0-as-specials.pipe';

describe('Season0AsSpecialsPipe', () => {
  it('create an instance', () => {
    const pipe = new Season0AsSpecialsPipe();
    expect(pipe).toBeTruthy();
  });
});
