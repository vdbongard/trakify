describe('TransitionGroupItemDirective', () => {
  it('should create an instance', () => {
    // const directive = new TransitionGroupItemDirective();
    // expect(directive).toBeTruthy();
  });
});
